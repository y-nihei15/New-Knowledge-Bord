const buttonReflect = document.querySelector('.ButtonReflect');
const buttonLogout = document.querySelector('.ButtonLogout');

buttonReflect.addEventListener('click', () => {
  alert('行先・コメントが反映されました。');
});

buttonLogout.addEventListener('click', () => {
  alert('ログアウトしました。');
});
